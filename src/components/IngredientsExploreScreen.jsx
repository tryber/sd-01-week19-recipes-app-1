import React from 'react';

const IngredientsExploreScreen = (...drinkOrMeal) => {
  const divWithIngredient = () => {
    drinkOrMeal.map(property => (
      //Fazer um request na API com vários ingredientes
      // De acordo com o toque do usuário, redirecionar para uma página que contenha receitas
      // com esse ingrediente escolhido
    ))
  };
};

export default IngredientsExploreScreen;
